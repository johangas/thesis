all: ex-6lowpannd-6lh
APPS=6lowpannd

APPS += serial-shell

PROJECT_SOURCEFILES += ../shell-6l.c 

CONTIKI_WITH_IPV6 = 1
CONTIKI_WITH_RPL = 0
UIP_CONF_6LN=1

ifeq ($(WITH_STATISTICS), 1)
	CFLAGS += -DUIP_CONF_STATISTICS=1
endif

CFLAGS += -DUIP_CONF_ND6_REGISTER_LIFETIME=5 -DTCPIP_CONF_ANNOTATE_TRANSMISSIONS=1

CONTIKI = ../../..
include $(CONTIKI)/Makefile.include
