all: ex-6lowpannd-6lr
APPS=6lowpannd


APPS += serial-shell
PROJECT_SOURCEFILES += ../shell-6l.c 

CONTIKI_WITH_IPV6 = 1
CONTIKI_WITH_RPL = 1
UIP_CONF_6LR=1

ifeq ($(WITH_STATISTICS), 1)
	CFLAGS += -DUIP_CONF_STATISTICS=1
endif

CFLAGS += -DUIP_CONF_ND6_RA_PERIODIC=0 -DTCPIP_CONF_ANNOTATE_TRANSMISSIONS=1
CFLAGS += -DUIP_CONF_MAX_ROUTES=100

CONTIKI = ../../..
include $(CONTIKI)/Makefile.include
